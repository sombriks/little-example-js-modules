// definind and also exporting the class as the default export symbol.
// unlike python, all module (file) members are private unless exported.
export default class DefaultPrinter {

	print() {
		console.log("printing using default printer impl")
	}
}